spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    datasource:
      driver-class-name: org.postgresql.Driver
      url:${POSTGRES_DB_URL}
      username: postgress
      password: asdqwe