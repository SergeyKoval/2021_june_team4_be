spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver

jwt:
  expiration:
    seconds:
      access: 120
      refresh: 3600